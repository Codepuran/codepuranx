# Leave unset during local development: Vite proxies API calls to Fastify on port 3000.
# Set this to the deployed API's public origin for a production deployment.
# VITE_API_URL=https://api.example.com
